/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains:["source.unsplash.com"],
    domains:["blogtalkbd.herokuapp.com"]
  },
};

module.exports = nextConfig
